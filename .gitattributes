# Файлы, которые исполняются на Linux-сервере, ВСЕГДА хранятся с LF.
# Иначе Windows-окончания строк (CRLF) ломают bash-скрипты и конфиги:
#   bash: setup.sh: /usr/bin/env bash^M: bad interpreter
*.sh        text eol=lf
*.service   text eol=lf
*.conf      text eol=lf
